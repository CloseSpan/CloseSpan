{
  "name": "@closespan/agent-executor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "check:deploy": "wrangler deploy --dry-run --outdir=/tmp/closespan-agent-executor-dry-run",
    "types": "wrangler types"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.114.0"
  }
}
